import 'package:better_hm/shared/components/card_with_title.dart';
import 'package:flutter/widgets.dart';

class AddCustomCalendarWidget extends StatelessWidget {
  const AddCustomCalendarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return CardWithTitle(title: "Add custom Calendar", child: Placeholder());
  }
}
